import { ThemDirective } from './them.directive';

describe('ThemDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemDirective();
    expect(directive).toBeTruthy();
  });
});
